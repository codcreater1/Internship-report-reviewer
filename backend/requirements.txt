# Python 3.12+ required

# --- Web ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
email-validator==2.3.0
pydantic==2.13.4
pydantic-settings==2.13.1
python-dotenv==1.2.2

# --- PDF ---
PyMuPDF==1.27.2.3      # read submitted documents, embed the signature
reportlab==4.2.5       # render the completion certificate
pillow==12.2.0         # normalise the signature image before embedding

# --- AI (advisory reading + email drafting only; the decision never uses it) ---
openai==2.41.0
# langfuse            # optional: uncomment to trace model calls

# --- Tests ---
pytest==8.3.4
httpx==0.28.1
