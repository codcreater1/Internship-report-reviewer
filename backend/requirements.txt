# Python 3.12+ required

# --- Web ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
email-validator==2.3.0
pydantic==2.13.4
pydantic-settings==2.13.1
python-dotenv==1.2.2

# --- PDF ---
PyMuPDF==1.27.2.3      # read submitted documents, embed the signature
reportlab==4.2.5       # render the completion certificate
pillow==12.2.0         # normalise the signature image before embedding

# --- AI (advisory reading + email drafting only; the decision never uses it) ---
openai==2.41.0

# The advisory reading is a three-step graph, not one call — see
# app/agents/review_graph.py. Pure Python, ~40 MB in the image.
langgraph==1.2.11

# Tracing. Dormant unless LANGFUSE_PUBLIC_KEY and LANGFUSE_SECRET_KEY are set,
# and app/core/llm.py swaps the client for the traced one at that point.
langfuse==4.15.1
